import { CustomLayout } from 'Layout/layout'

const SettingsPage = () => {
  return (
    <CustomLayout title="Settings">
      <h1>Settings page</h1>
    </CustomLayout>
  )
}

export default SettingsPage
